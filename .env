# .env es el archivo donde se guardan variables de 
# configuración sensibles que no queremos escribir 
# directamente en el código (variables de entorno).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=admin #contraseña que hayan puesto en mysql
DB_NAME=uninorte
SECRET_KEY=universidad_caribe_secret_2024
